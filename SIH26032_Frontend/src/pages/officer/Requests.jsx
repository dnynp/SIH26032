import { useState } from "react";
import Layout from "../../components/Layout";
import StatusBadge from "../../components/StatusBadge";
const initial=[{token:21,farmer:"Yash",crop:"Rice",qty:"300 kg",center:"Pune",status:"Pending"},{token:22,farmer:"Rahul",crop:"Wheat",qty:"500 kg",center:"Pune",status:"Approved"},{token:23,farmer:"Suresh",crop:"Cotton",qty:"700 kg",center:"Nashik",status:"Scheduled"}];
export default function Requests(){const [rows,setRows]=useState(initial);const act=(i,status)=>setRows(rows.map((r,x)=>x===i?{...r,status}:r));return <Layout><div className="page-head"><div><p className="eyebrow">WORK QUEUE</p><h1>Procurement Requests</h1></div><select className="filter"><option>All Statuses</option><option>Pending</option><option>Approved</option><option>Scheduled</option></select></div><div className="card table-card"><table><thead><tr><th>Token</th><th>Farmer</th><th>Crop</th><th>Quantity</th><th>Center</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map((r,i)=><tr key={r.token}><td><b>#{r.token}</b></td><td>{r.farmer}</td><td>{r.crop}</td><td>{r.qty}</td><td>{r.center}</td><td><StatusBadge status={r.status}/></td><td>{r.status==="Pending"&&<div className="actions"><button className="btn small primary" onClick={()=>act(i,"Approved")}>Approve</button><button className="btn small danger" onClick={()=>act(i,"Rejected")}>Reject</button></div>}{r.status==="Approved"&&<button className="btn small primary" onClick={()=>act(i,"Scheduled")}>Schedule</button>}{r.status==="Scheduled"&&<button className="btn small primary" onClick={()=>act(i,"Procured")}>Mark Procured</button>}{r.status==="Procured"&&<span className="muted">Completed</span>}</td></tr>)}</tbody></table></div></Layout>}
